# Backend runtime (TDD-06). Torch/transformers/librosa are pulled in via the
# ASR requirements (default STT path loads the fine-tuned Whisper).
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
python-multipart>=0.0.9   # multipart/form-data for audio uploads
python-dotenv>=1.0          # loads backend/.env at startup

# Agent (TDD-02): LangGraph state graph + tool-calling. The default LLM provider
# is offline (no key); openai/groq are optional, lazy-imported when selected.
langgraph>=1.0
langchain-core>=1.0
# anthropic>=0.40  # uncomment when LLM_PROVIDER=anthropic (Claude)
# openai>=1.0     # uncomment when LLM_PROVIDER=openai
# groq>=0.11      # uncomment when LLM_PROVIDER=groq (also the Anthropic fallback)

# Knowledge base + RAG (TDD-04): YAML data packs + a retriever behind an interface.
# The default retriever is "chroma" (real multilingual embeddings, runs locally on
# CPU — no key); tests use the dependency-free "keyword" retriever (conftest).
pyyaml>=6.0
chromadb>=1.5            # vector store for the faq RAG (KB_RETRIEVER=chroma)
sentence-transformers>=5.0  # multilingual embeddings (reuses the torch already pulled by ASR)

# ASR deps (torch, transformers, librosa) — required for the default STT path:
#   pip install -r ../asr_finetuning/requirements.txt
